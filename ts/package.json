{
  "name": "@voxgig-sdk/bng2latlong-converter",
  "version": "0.0.1",
  "description": "DESCRIPTION",
  "main": "dist/Bng2latlongConverterSDK.js",
  "type": "commonjs",
  "types": "dist/Bng2latlongConverterSDK.d.ts",
  "scripts": {
    "test": "node --enable-source-maps --test-concurrency=1 --test 'dist-test/**/*.test.js'",
    "test-some": "node --enable-source-maps --experimental-test-isolation=none --test-name-pattern=\"$TEST_PATTERN\" --test 'dist-test/**/*.test.js'",
    "test-utility": "node --enable-source-maps --test test/utility/*.test.ts",
    "watch": "tsc --build src test -w",
    "build": "tsc --build src test",
    "clean": "rm -rf node_modules yarn.lock package-lock.json",
    "reset": "npm run clean && npm i && npm run build && npm test"
  },
  "author": "Bng2latlongConverter",
  "license": "MIT",
  "dependencies": {},
  "peerDependencies": {},
  "devDependencies": {
    "dotenv": "^17.4.2",
    "typescript": "^5.9.3",
    "@types/node": "^25.6.0"
  }
}